# Project Context

이 저장소는 C++ / DirectX 11 / CMake 기반의 학습용 게임 엔진 프로젝트입니다.

## Working Style

- 답변과 작업은 구현 결과만 제시하기보다, 왜 그렇게 구성하는지와 Direct3D 파이프라인에서 어떤 의미인지 설명하는 방향을 우선합니다.
- 참고 서적의 예제 코드는 구 DirectX SDK / D3DX / Effects11 기반인 경우가 많으므로, 현대 DirectX 11과 현재 일반적으로 많이 쓰는 방식으로 바꾸어 설명합니다.
- 책 코드가 왜 동작했는지와 현재 방식에서 왜 바뀌어야 하는지를 함께 설명합니다.
- 파일을 직접 수정하는 것은 사용자가 명시적으로 요청한 경우를 제외하고 최대한 지양합니다. 기본적으로는 사용자가 직접 작성할 수 있도록 방향, 예시, 검토 기준을 제공합니다.
- 학습과 관련된 질문은 정답을 바로 완성 형태로 제시하기보다, 먼저 개념과 판단 기준을 설명하고 사용자가 생각해볼 수 있도록 유도합니다.
- API, 헤더, 함수 역할, 설정 경로처럼 레퍼런스 성격이 강한 질문에는 불필요하게 돌려 묻지 않고 바로 명확한 답을 제공합니다.
- 학습 흐름이 보이도록 과도한 자동화, 과도한 추상화, 너무 이른 공용화를 피합니다.

## Project Structure

- `Src/Engine`: DirectX를 몰라도 되는 순수 엔진 계층입니다. 수학, 물리, 게임 로직에 가까운 코드를 둡니다.
- `Src/Framework`: Win32, Direct3D 11, 렌더링 초기화, 셰이더, 버퍼, DirectX 타입 변환 같은 플랫폼/API 의존 코드를 둡니다.
- `Src/Exercises`: 챕터별 학습 코드와 실험 코드를 둡니다.

## Coding Guidelines

- `Engine` 계층에는 가능하면 `Windows.h`, `d3d11.h`, `DirectXMath` 같은 DirectX/Win32 의존성을 넣지 않습니다.
- DirectX 타입을 받거나 반환하는 유틸리티는 `Framework` 계층에 둡니다.
- HLSL 런타임 컴파일 단계에서는 `.hlsl` 소스 하나를 기준으로 직접 컴파일하며, 나중에 필요해지면 `.cso` 빌드타임 컴파일로 전환합니다.
- 기존 사용자의 변경이나 실험 파일은 명시 요청 없이 되돌리지 않습니다.
